Rounded surface container with a warm shadow. `interactive` adds a hover lift for clickable cards.

```jsx
import { Card } from "../core/Card";

<Card>Static content</Card>
<Card interactive onClick={open}>Clickable</Card>
<Card tone="gold" padding="var(--space-6)">Highlighted</Card>
```
