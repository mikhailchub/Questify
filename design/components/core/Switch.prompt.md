Toggle switch for settings — themes, notification channels, silent mode.

```jsx
import { Switch } from "../core/Switch";

<Switch label="Dark mode" checked={dark} onChange={setDark} />
<Switch label="Slack notifications" defaultChecked />
```
