Currency counter chip with a minted coin glyph. `gold` for ⚔️ Gold, `token` for 🎟️ Guild Tokens. Use in the top bar and Reward Forge.

```jsx
import { CurrencyPill } from "../game/CurrencyPill";

<CurrencyPill type="gold" amount={1240} />
<CurrencyPill type="token" amount={6} size="sm" />
```
