Player avatar with a gold level ring and optional corner level chip. Falls back to initials when no `src`.

```jsx
import { Avatar } from "../core/Avatar";

<Avatar name="Ada Lovelace" level={14} size={56} />
<Avatar src="/me.png" name="Ada" size={36} ring={false} />
```
